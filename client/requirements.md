## Packages
framer-motion | Smooth animations for the landing page
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The application serves a Stremio addon manifest at /manifest.json
The frontend acts as an installation landing page.
Installation links need to use the `stremio://` protocol dynamically based on window.location.
